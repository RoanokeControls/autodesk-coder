{
  "id": "SimpleExtrusionSample_Sample",
  "title": "Simple Extrusion Sample",
  "type": "sample",
  "namespace": "",
  "header_file": "",
  "description": "Creates a new extrusion feature, resulting in a new component.",
  "parent_class": "",
  "code_samples": {
    "python": "import adsk.core, adsk.fusion, traceback\n\ndef run(context):\n    ui = None\n    try: \n        app = adsk.core.Application.get()\n        ui = app.userInterface\n\n        doc = app.documents.add(adsk.core.DocumentTypes.FusionDesignDocumentType)\n        design = app.activeProduct\n\n        # Get the root component of the active design.\n        rootComp = design.rootComponent\n\n        # Create a new sketch on the xy plane.\n        sketches = rootComp.sketches\n        xyPlane = rootComp.xYConstructionPlane\n        sketch = sketches.add(xyPlane)\n\n        # Draw a circle.\n        circles = sketch.sketchCurves.sketchCircles\n        circle1 = circles.addByCenterRadius(adsk.core.Point3D.create(0, 0, 0), 2)\n\n        # Get the profile defined by the circle.\n        prof = sketch.profiles.item(0)\n\n        # Define that the extent is a distance extent of 5 cm.\n        distance = adsk.core.ValueInput.createByReal(5)\n\n        # Create the extrusion.\n        extrudes = rootComp.features.extrudeFeatures\n        ext = extrudes.addSimple(prof, distance, adsk.fusion.FeatureOperations.NewComponentFeatureOperation)\n    except:\n        if ui:\n            ui.messageBox('Failed:\\n{}'.format(traceback.format_exc()))",
    "cpp": "#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n#include \n\nusing namespace adsk::core;\nusing namespace adsk::fusion;\n\nPtr ui;\n\nextern \"C\" XI_EXPORT bool run(const char* context)\n{\n    Ptr app = Application::get();\n    if (!app)\n        return false;\n\n    ui = app->userInterface();\n    if (!ui)\n        return false;\n\n    Ptr documents = app->documents();\n    if (!documents)\n        return false;\n\n    Ptr doc = documents->add(DocumentTypes::FusionDesignDocumentType);\n    if (!doc)\n        return false;\n\n    Ptr product = app->activeProduct();\n    if (!product)\n        return false;\n\n    Ptr design = product;\n    if (!design)\n        return false;\n\n    // Get the root component of the active design\n    Ptr rootComp = design->rootComponent();\n    if (!rootComp)\n        return false;\n\n    // Create a new sketch on the xy plane.\n    Ptr sketches = rootComp->sketches();\n    if (!sketches)\n        return false;\n    Ptr xyPlane = rootComp->xYConstructionPlane();\n    if (!xyPlane)\n        return false;\n    Ptr sketch = sketches->add(xyPlane);\n    if (!sketch)\n        return false;\n\n    // Draw a circle.\n    Ptr sketchCurves = sketch->sketchCurves();\n    if (!sketchCurves)\n        return false;\n    Ptr circles = sketchCurves->sketchCircles();\n    if (!circles)\n        return false;\n    Ptr centerPoint = Point3D::create(0, 0, 0);\n    if (!centerPoint)\n        return false;\n    Ptr circle1 = circles->addByCenterRadius(centerPoint, 2);\n    if (!circle1)\n        return false;\n\n    // Get the profile defined by the circle.\n    Ptr profs = sketch->profiles();\n    if (!profs)\n        return false;\n    Ptr prof = profs->item(0);\n    if (!prof)\n        return false;\n\n    // Define that the extent is a distance extent of 5 cm.\n    Ptr distance = ValueInput::createByReal(5);\n    if (!distance)\n        return false;\n\n    // Create the extrusion.\n    Ptr feats = rootComp->features();\n    if (!feats)\n        return false;\n    Ptr extrudes = feats->extrudeFeatures();\n    if (!extrudes)\n        return false;\n    Ptr ext = extrudes->addSimple(prof, distance, FeatureOperations::NewComponentFeatureOperation);\n    if (!ext)\n        return false;\n\n    return true;\n}"
  }
}